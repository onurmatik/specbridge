# Superdesign References

These assets were fetched from Superdesign project `0e542410-b834-442c-98b7-89e31fdcbbd0` and checked into the repo so UI implementation can stay anchored to the approved design drafts.

## Drafts
- `workspace.html` / `workspace.png`
- `dashboard.html` / `dashboard.png`
- `decisions.html` / `decisions.png`
- `history.html` / `history.png`
- `handoff.html` / `handoff.png`

## Notes
- The runtime implementation uses Django templates and Tailwind CSS, but these references preserve the original HTML and screenshots from Superdesign.
- Added pages (`assumptions`, `members`, and the project directory) intentionally follow the same design system but do not have original Superdesign drafts.

